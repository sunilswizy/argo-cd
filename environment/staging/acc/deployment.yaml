---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: acc
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: sunil1912/nginx:2.2
          ports:
            - containerPort: 80